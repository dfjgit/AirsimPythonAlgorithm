# 项目概述

> 这个文件用于记录项目的概述信息，帮助 Claude 快速了解项目。

## 项目名称

AirSim Python Algorithm

## 项目描述

基于 AirSim + Unity 的多无人机协同控制与训练系统，支持 APF、DDPG+APF、DQN Movement 等算法，提供训练日志分析、可视化和诊断工具。

## 技术栈

- Python 3.8+、stable-baselines3、gym、numpy、pandas、matplotlib、seaborn、opencv-python、pygame
- AirSim 模拟器 + Unity 场景
- Crazyflie 实体无人机支持

## 目录结构

```text
multirotor/
  AlgorithmServer.py                 主服务入口（管理 AirSim/Unity/算法线程）
  system_config.json                 统一系统配置（无人机/算法/环境/训练）
  Algorithm/                         APF 扫描算法、六角网格、数据采集、碰撞/训练分析
  AirsimServer/                      AirSim 与 Unity 通信层
  Crazyswarm/                        Crazyflie 实体无人机支持
  DDPG_Weight/                       DDPG 权重训练模块
    configs/unified_train_config.json  DDPG 训练统一配置
  DQN_Movement/                      DQN 移动控制 + 分层 DQN
    configs/                           算法专属配置（继承 system_config.json）
  Visualization/                     pygame 面板式可视化模块
  tests/                             单元测试
```

## 配置体系

采用统一配置架构，从 15 个配置文件精简为 5 个：

| 配置文件 | 职责 |
|---|---|
| `system_config.json` | 统一入口：无人机定义、APF 算法参数、环境规则、训练选择 |
| `DDPG_Weight/configs/unified_train_config.json` | DDPG 训练：4 种模式（虚拟/在线/离线日志/混合） |
| `DDPG_Weight/configs/crazyflie_reward_config.json` | Crazyflie 奖励配置 |
| `DQN_Movement/configs/movement_dqn_config.json` | DQN Movement 专属参数 |
| `DQN_Movement/configs/hierarchical_dqn_config.json` | 分层 DQN 专属参数 |

## 核心架构

- **ScannerAlgorithm**（`Algorithm/scanner_algorithm.py`）：唯一的 APF 力计算模块
  - 纯 APF 模式：使用 system_config.json 中的固定权重
  - DDPG+APF 模式：DDPG 模型每帧动态预测 5 个 APF 权重系数
- **AlgorithmServer**：主服务，通过 `control_mode`（apf/dqn）和 `use_learned_weights` 切换模式
- **SystemConfig / DronesConfig / ScannerConfigData**：三层配置加载类，统一从 system_config.json 读取

## 开发说明

- 配置修改只需编辑 `system_config.json`，无需关心其他配置文件
- DQN 算法专属的 termination/battery 在运行时从 system_config.json 继承，可被本地配置覆盖
- ScannerConfigData 支持新旧两种 JSON 格式自动识别（向后兼容）
