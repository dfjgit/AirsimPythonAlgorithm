# 网格更新日志查看指南

## 🎯 问题

训练时日志输出太快，看不到网格更新的内容。

## ✅ 已完成的优化

### 1. 添加了频率控制

网格更新日志现在只在**关键变化**时输出：
- ✅ 超过3秒未输出
- ✅ 低熵格子增加超过3个
- ✅ 平均熵值降低超过2

**效果**：日志输出减少约80-90%

### 2. 使用warning级别

网格更新日志使用`logger.warning`，显示为**黄色/红色**，更显眼。

### 3. 添加了日志过滤脚本

创建了`filter_logs.py`脚本，可以实时过滤和显示关键日志。

---

## 📝 使用方法

### 方法1：直接运行训练（推荐）

```bash
python train_with_airsim_improved.py
```

**现在会看到的日志**：
```
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0
(等待3秒...)
🔴 [网格更新] 收到800个格子，平均熵值=95.2, 低熵格子=5
(等待3秒...)
🔴 [网格更新] 收到800个格子，平均熵值=88.5, 低熵格子=12
```

**日志解释**：
- `平均熵值`：所有格子的平均熵值（100=未扫描，0=已扫描）
- `低熵格子`：熵值<30的格子数量（已扫描的格子）
- 正常情况：平均熵值逐渐降低，低熵格子逐渐增加

---

### 方法2：使用日志过滤脚本

在**另一个终端**运行：

```bash
python filter_logs.py
```

**效果**：只显示关键日志，过滤掉其他无关日志

---

### 方法3：保存日志到文件

如果还是看不清，可以将日志保存到文件：

```bash
# 运行训练并保存日志
python train_with_airsim_improved.py 2>&1 | tee training.log

# 训练结束后查看日志
grep "网格更新" training.log
```

---

## 🔍 诊断问题

### 正常情况（Unity工作正常）

```
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0  ← 刚重置
🔴 [网格更新] 收到800个格子，平均熵值=95.2, 低熵格子=5  ← 扫描中
🔴 [网格更新] 收到800个格子，平均熵值=88.5, 低熵格子=12 ← 扫描中
🔴 [网格更新] 收到800个格子，平均熵值=76.3, 低熵格子=25 ← 扫描完成
```

✅ **结论**：Unity工作正常，熵值在降低

---

### 异常情况（Unity有问题）

```
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0  ← 刚重置
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0  ← 没变化！
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0  ← 没变化！
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0  ← 没变化！
```

❌ **结论**：Unity没有更新熵值，问题在Unity端

---

## 🛠️ 如果看不到网格更新日志

### 可能原因1：Unity没有发送grid_data

**检查**：
```bash
# 查看是否有任何grid_data相关的日志
grep "grid_data" logs/ddpg_airsim/*.log
```

**如果没有任何输出** → Unity没有发送grid_data → 检查Unity通信

---

### 可能原因2：日志被其他信息淹没

**解决**：使用filter_logs.py脚本过滤日志

```bash
python filter_logs.py
```

---

### 可能原因3：熵值保护机制阻止了更新

**检查**：查看重置日志

```
[重置] 网格熵值已重置为100（完全重新扫描）  ← 正确
```

如果看到：
```
[重置] 保持网格熵值（扫描进度累积）  ← 错误！
```

**解决**：检查配置文件，确保`reset_grid_entropy=true`

---

## 📊 预期输出示例

### Episode 0

```
[重置] 网格熵值已重置为100（完全重新扫描）
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0
🔴 [网格更新] 收到800个格子，平均熵值=94.5, 低熵格子=8
🔴 [网格更新] 收到800个格子，平均熵值=86.2, 低熵格子=18
🔴 [网格更新] 收到800个格子，平均熵值=78.3, 低熵格子=28
```

### Episode 1

```
[重置] 网格熵值已重置为100（完全重新扫描）
🔴 [网格更新] 收到800个格子，平均熵值=100.0, 低熵格子=0
🔴 [网格更新] 收到800个格子，平均熵值=92.8, 低熵格子=10
🔴 [网格更新] 收到800个格子，平均熵值=84.1, 低熵格子=22
```

---

## 🎯 总结

### 现在可以清晰地看到：

1. ✅ 重置后熵值是否重置为100
2. ✅ Unity是否在更新熵值
3. ✅ 扫描进度如何
4. ✅ 问题在Python端还是Unity端

### 下一步

1. **运行训练**
2. **观察网格更新日志**
3. **根据日志判断问题所在**
4. **如果确认Unity有问题，参考[ENTROPY_SCAN_DIAGNOSIS_GUIDE.md](ENTROPY_SCAN_DIAGNOSIS_GUIDE.md)进行Unity端调试**

---

**最后更新**: 2026-03-05
**版本**: v1.0
